# Endpoints

## Tasks

- `GET /tasks` - Returns all tasks which are not done yet
- `POST /tasks/{taskId}/complete` - Marks the given task as completed
- `POST /tasks/{taskId}/undo` - Marks the given task as not completed
